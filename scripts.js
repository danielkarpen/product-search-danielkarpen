import { catalog } from "./lib/index.js";

console.log(catalog);

// TODO: Dynamically render the data into a table using the 'manual HTML' as a guide.

// TODO: Apply filtering to show hide/re-render the table accordingly.
